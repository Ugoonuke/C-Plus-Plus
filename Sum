#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int a, b, c, sum;
    scanf("%d%d%d", &a, &b, &c); 
    sum = a + b +c;
    printf("%d", sum);
    return 0;
}
